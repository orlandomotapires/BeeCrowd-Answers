#include <stdio.h>
#include <stdilib.h>
int main(){
  int linhas, colunas, compMin, compMax;
  scanf("%d %d %d %d", &linhas, &colunas, &compMin, &compMax);
  int plantacao[linhas][colunas];

  return 0;
}